import "./modal.scss";
import PropTypes from "prop-types";

const Modal = (props) => {
  return <div></div>;
};

Modal.prototype = {
  active: PropTypes.bool,
  id: PropTypes.string,
};

export default Modal;
